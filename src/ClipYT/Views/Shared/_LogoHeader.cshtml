@using ClipYT.Interfaces;
@inject IRandomCaptionService RandomCaptionService;

<div class="d-flex justify-content-center mb-4">
        <a class="logo-a" href=@Url.Action("Index") title="@ViewBag.Version">
            <img src="~/images/clipyt_logo_vectorized.svg" alt="ClipYT logo" id="logo-img" />
            <span class="logo-caption">@RandomCaptionService.GetRandomCaption()</span>
        </a>
</div>

<style>
    .logo-a {
        text-decoration: none !important;
        position: relative;
    }

    #logo-img {
        max-height: 100px;
        width: auto;
        height: auto;
    }

    .logo-caption {
        font-family: Nekst;
        color: var(--accent-color);
        position: absolute;
        left: 77px;
        top: 77px;
        line-height: 15px;
        font-size: medium;
    }
</style>